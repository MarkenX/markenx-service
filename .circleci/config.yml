version: 2.1

orbs:
  email: cinotify/email@1.2.1

defaults: &job_defaults
  docker:
    - image: 'cimg/openjdk:17.0'
  working_directory: ~/project
  steps:
    - checkout

jobs:
  build:
    <<: *job_defaults
    steps:
      - checkout
      - run:
          name: Build Application
          command: mvn clean package -DskipTests

  test:
    <<: *job_defaults
    steps:
      - checkout
      - run:
          name: Run Tests
          command: mvn test
      - store_test_results:
          path: target/surefire-reports

  sonar:
    <<: *job_defaults
    steps:
      - checkout
      - run:
          name: Analyze on SonarQube Cloud
          command: mvn verify sonar:sonar -Dsonar.projectKey=ChrisJMora_udla-markenx-service -Dsonar.organization=chrisjmora -Dsonar.host.url=https://sonarcloud.io

  send_email:
    docker:
      - image: cimg/base:stable
    steps:
      - email/send:
          to: $EMAIL_TO
          subject: $EMAIL_SUBJECT
          body: $EMAIL_BODY

workflows:
  main:
    jobs:
      - build
      - test:
          requires:
            - build
      - sonar:
          requires:
            - build
          context: SonarCloud
          filters:
            branches:
              only: main
      - send_email:
          requires:
            - test
            - sonar
