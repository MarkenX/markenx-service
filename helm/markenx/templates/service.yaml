apiVersion: v1
kind: Service
metadata:
  name: {{ include "markenx.fullname" . }}
  labels:
    {{- include "markenx.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "markenx.selectorLabels" . | nindent 4 }}
