package com.udla.markenx.domain.model;

public class Student extends Person {

    public Student(
        long id,
        String name,
        String lastname
    ) {
        super(id, name, lastname);
    }
}
